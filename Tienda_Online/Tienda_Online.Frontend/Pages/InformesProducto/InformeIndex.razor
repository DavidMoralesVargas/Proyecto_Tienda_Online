@page "/informes"

<h1>Informes de producto más consumidos:</h1>

<div class="col-md-9">
    <Pagination CurrentPage="currentPage"
                TotalPages="totalPages"
                SelectedPage="SelectedPageAsync" />
</div>

<div class="mb-2" style="display: flex; flex-wrap:wrap; align-items: center;">
    <input style="width: 400px; background:#D9D9D9" type="text" class="form-control" id="titulo" placeholder="Buscar producto..."
           @bind-value="Filtro" />
    <button type="button" class="btn btn-outline-primary mx-1" @onclick="ApplyFilterAsync">Filtrar</button>
    <button type="button" class="btn btn-outline-danger" @onclick="CleanFilterAsync">Limpiar</button>
</div>
<GenericList MyList="informes">
    <Body>
        

        <div class="mb-3">
            <div class="row">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Producto</th>
                            <th>Cantidad pedida</th>
                            <th>Ganancia</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach(var informe in informes!)
                        {
                            <tr>
                                <td>@informe.Producto</td>
                                <td>@informe.CantidadPedido</td>
                                <td>@informe.Ganacias</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </Body>
</GenericList>